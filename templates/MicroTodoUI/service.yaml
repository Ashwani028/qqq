apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-todo-ui
spec:
  type: {{ .Values.todoui.image.type }}
  selector:
    app: {{ .Release.Name }}-todo-ui
  ports:
    - port: {{ .Values.todoui.image.ports.port }}
      targetPort: {{ .Values.todoui.image.ports.targetPort }}
